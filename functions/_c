#compdef c

for directory in "${RESOURCES}/Repositories"/*; do
	_files -W "${directory}" -/
done

_files -W "${RESOURCES}/Repositories" -/
_files -W "${PROJECTS}" -/
_files -W "${HOME}/Documents" -/

# See further documentation in `functions/c`.
